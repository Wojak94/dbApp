<%= form_for(@serviceman) do |f| %>
  <% if @serviceman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@serviceman.errors.count, "error") %> prohibited this serviceman from being saved:</h2>

      <ul>
      <% @serviceman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
